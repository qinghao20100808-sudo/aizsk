# AI 实时受力分析系统 — 项目记忆

> 基于计算机视觉和物理推理的智能高中物理教学辅助工具。

---

## 一、项目概况

| 项目 | 内容 |
|------|------|
| 项目名称 | AI 实时受力分析系统（AI 实时受力分析系统） |
| 版本 | v3.0 |
| 技术栈 | Python 3.14 + Flask + YOLOv11 + OpenCV + PyTorch (CPU) |
| 运行环境 | WSL2 (Ubuntu 26.04) + Windows 11 宿主机 |
| 摄像头 | Webcam C110（Windows 端），通过共享文件模式传输到 WSL |
| 当前状态 | 开发中，核心功能可用 |

---

## 二、系统架构

### 前端（浏览器端）
- **纯 Canvas 渲染**：不直接显示 `<video>` 元素，使用隐藏的 `<video>`（`capVideo`，`opacity:0` 在视口内）接收摄像头流
- 使用 `requestAnimationFrame` 循环将视频帧绘制到 `displayCanvas`
- 检测框、受力箭头、速度向量全部在同一 Canvas 上绘制
- 点击事件直接在 Canvas 上监听

### 后端（Flask 服务器）
- `--web` 模式：浏览器负责摄像头，后端只做 AI 推理（推荐模式）
- `--demo` 模式：模拟场景演示，无需摄像头
- `--win-cam` 模式：WSL + Windows 共享文件摄像头
- 核心 API：`/api/detect`、`/api/detect/start`、`/api/click`、`/api/scene`、`/api/status`

### 目录结构
```
aizsk/
├── aizsk/
│   ├── detector.py      # YOLO 目标检测
│   ├── tracker.py        # 目标跟踪（IoU 匹配）
│   ├── physics.py        # 物理推理引擎
│   ├── scene.py          # 场景理解
│   ├── visualizer.py     # 力箭头绘制
│   ├── simulation.py     # 模拟场景
│   ├── web_server.py     # Flask 服务器 + API
│   ├── main.py           # 入口
│   └── static/
│       └── index.html    # 前端（纯 Canvas 模式）
├── pyproject.toml
├── README.md
└── uv.lock
```

---

## 三、运行模式

### 3.1 Web 模式（推荐）
```bash
uv run python3 -m aizsk.main --web
```
- 浏览器使用 `navigator.mediaDevices.getUserMedia()` 捕获摄像头
- 前端 500ms 定时发送帧到后端 `/api/detect`
- 后端运行 YOLO 检测 + 跟踪 + 物理推理
- `conf_threshold=0.05`（超低阈值，识别小物体）
- 点击 Canvas 任何位置 → 后端创建"虚拟物体"兜底，始终有反馈

### 3.2 演示模式
```bash
uv run python3 -m aizsk.main --demo incline   # 斜面滑块
uv run python3 -m aizsk.main --demo flat_push # 水平推动
uv run python3 -m aizsk.main --demo free_fall # 自由落体
uv run python3 -m aizsk.main --demo pendulum  # 单摆
```

### 3.3 Windows 共享文件摄像头模式
```bash
# 1. 先启动 Windows 端 ffmpeg（在 PowerShell 中）
Start-Process -FilePath 'C:\Users\34931\ffmpeg\bin\ffmpeg.exe' -ArgumentList @(
    '-f', 'dshow',
    '-i', 'video=Webcam C110',
    '-vf', 'fps=10', '-q:v', '10',
    '-update', '1',
    'C:\Users\34931\AppData\Local\Temp\live_cam_hermes.jpg'
) -WindowStyle Hidden

# 2. 启动服务器
uv run python3 -m aizsk.main --win-cam
```

---

## 四、API 接口

### `POST /api/detect/start`
初始化 YOLO 模型，重置跟踪器。

### `POST /api/detect`
接收前端摄像头帧进行处理。

**请求**：`multipart/form-data`
| 字段 | 类型 | 说明 |
|------|------|------|
| `image` | file | JPEG 图片 |
| `click_x` | int | 可选，点击 X 坐标 |
| `click_y` | int | 可选，点击 Y 坐标 |
| `scene` | string | 场景类型：`flat`/`incline`/`free_fall`/`pendulum` |
| `angle` | float | 斜面角度 |
| `mass` | float | 物体质量（kg），0=未知 |

**返回**：
```json
{
  "success": true,
  "detections": [{"track_id": 1, "class_name": "bottle", "confidence": 0.85, "bbox": [x1,y1,x2,y2], "selected": false}],
  "selected_id": 1,
  "analysis": {
    "scene": "flat",
    "note": "物体在水平面上静止",
    "forces": [{"type": "gravity", "magnitude": 9.8, "angle": 90, "label": "G", "color": [0,0,255]}],
    "net_force": {"magnitude": 0, "angle": 0, "label": "合力"}
  },
  "velocity": {"vx": 0, "vy": 0, "speed": 0, "is_moving": false}
}
```

### `POST /api/scene`
设置场景类型。

### `GET /api/status`
获取服务器状态。

---

## 五、物理推理引擎

### 支持场景
| 场景 | 力的分析 |
|------|---------|
| 水平面静止 | 重力↓、支持力↑ |
| 水平面运动 | 重力↓、支持力↑、摩擦力←→、外力→ |
| 斜面 θ° | 重力↓、支持力⊥斜面、摩擦力∥斜面 |
| 自由落体 | 重力↓ |
| 悬挂/单摆 | 重力↓、拉力↑ |

### 质量处理
- 用户不填质量：只显示力的方向（`G`、`N`），不显示数值
- 用户填写质量：显示实际 N 值（`G=49.0N`）

---

## 六、物体过滤机制

### 黑名单模式（当前）
只过滤以下类别的物体，其余全部保留：
- 人/动物：person, bird, cat, dog, horse, etc.
- 家具：bed, chair, couch, dining_table, toilet
- 电器：tv, laptop, keyboard, mouse, refrigerator, etc.
- 交通工具：car, bus, train, airplane, etc.
- 大型物品：backpack, handbag, suitcase, potted_plant, vase

### 历史
- **v1（白名单）**：只保留 bottle, cup, knife, book 等小物件
  - **问题**：用户手持的物体不在白名单时完全无检测
- **v2（黑名单）**：过滤家具/人/车/动物，其余全保留 ← 当前

---

## 七、YOLO 模型

| 模型 | 大小 | 速度 | 精度 | 状态 |
|------|------|------|------|------|
| yolo11n (nano) | ~5MB | 最快 | 最低 | ✅ 当前使用 |
| yolo11s (small) | ~18MB | 中等 | 中等 | ❌ 下载太慢放弃 |

**置信度阈值**：`0.05`（超低，识别小/遮挡物体）

---

## 八、前端交互设计演进

### v1（初始）：video + canvas 覆盖层 + click-layer
- `<video>` 显示摄像头，`<canvas>` 覆盖绘制检测框
- `<div.click-layer>` 处理点击事件
- **问题**：浏览器显示 PIP、全屏按钮；video 拦截右键菜单；点击层坐标计算复杂

### v2：移除控件，添加 disablePictureInPicture
- 添加 `disablePictureInPicture`、`pointer-events:none`
- 视频元素不再可交互
- **问题**：浏览器在某些情况下仍显示控件

### v3（当前）：纯 Canvas 渲染
- 隐藏 `<video id="capVideo">`（`opacity:0` 在视口内）
- 单 Canvas 绘制：摄像头帧 + 检测框 + 受力箭头
- 点击直接由 Canvas 处理
- `requestAnimationFrame` 连续渲染

### 关键 Bug 修复
1. **视频被浏览器暂停**：`hiddenVideo` 放在屏幕外（`top:-9999px`）导致浏览器停止解码 → 改为 `capVideo` 在视口内 `opacity:0`
2. **点击无反应**：浏览器暂停视频后 `videoWidth=0`，`sendDetect` 提前 return → 修复同上
3. **检测不到手持物体**：白名单太窄 → 改为黑名单；阈值从 0.4 降到 0.05

---

## 九、已知问题

### 9.1 检测相关
| 问题 | 原因 | 状态 |
|------|------|------|
| 手持物体检测率低 | YOLO 小模型能力有限，手部遮挡 | ⚠️ 已降至 0.05 阈值 |
| 识别不到钥匙/笔等小物 | 不在 COCO 数据集 80 类中 | ❌ 无法解决 |
| 检测框抖动 | YOLO 帧间不稳定 | ⚠️ 跟踪器缓解 |

### 9.2 前端相关
| 问题 | 原因 | 状态 |
|------|------|------|
| 高分辨率卡顿 | 4K 帧 JPEG 编码+网络传输开销 | ⚠️ 降采样处理 |
| Canvas 渲染闪烁 | requestAnimationFrame 与 toBlob 异步 | ✅ 已修复 |

### 9.3 后端相关
| 问题 | 原因 | 状态 |
|------|------|------|
| torchvision NMS 崩溃 | torch 2.13 + torchvision 0.28 + Python 3.14 不兼容 | ✅ 已 patch site-packages |
| NNPACK 警告 | CPU-only torch 无 NNPACK 支持 | ✅ 不影响功能 |

---

## 十、Windows 摄像头打通方案

由于 WSL2 无法直接访问 Windows 的 USB 摄像头，使用以下方案：

1. **ffmpeg 采集**：Windows 端运行 ffmpeg 从 DirectShow 捕获摄像头
2. **共享文件**：ffmpeg 使用 `-update 1` 模式，持续写入同一 JPEG 文件
3. **WSL 读取**：Python 通过 `/mnt/c/Users/.../Temp/live_cam_hermes.jpg` 读取
4. **OpenCV 解码**：`cv2.imread()` 读取 JPEG 为 numpy 数组

**已知限制**：
- 必须先在 Windows 端启动 ffmpeg
- 文件读写有 ~50ms 延迟
- 帧率受限（10fps）

---

## 十一、安装与部署

### 依赖安装
```bash
# 安装 uv
curl -LsSf https://astral.sh/uv/install.sh | sh

# 创建项目
cd /path/to/aizsk
uv sync --default-index https://pypi.tuna.tsinghua.edu.cn/simple

# 修复 torchvision 兼容性（Python 3.14 需要）
patch .venv/lib/python3.14/site-packages/ultralytics/nn/autobackend.py \
  -i /path/to/warmup_patch.txt
```

### warmup_patch.txt 内容
```diff
--- a/autobackend.py
+++ b/autobackend.py
@@ -314,7 +314,10 @@
         from ultralytics.utils.nms import non_max_suppression
 
         if not self.end2end:
-            import torchvision  # noqa
+            try:
+                import torchvision  # noqa
+            except RuntimeError:
+                pass  # version mismatch
```

### 启动
```bash
# 推荐：浏览器摄像头模式
uv run python3 -m aizsk.main --web

# 演示模式
uv run python3 -m aizsk.main --demo incline
```

---

## 十二、Git 提交历史

```
a37df92 修复：capVideo 放在视口内（opacity:0）而非屏幕外，防止浏览器暂停
fb46385 重写：纯 Canvas 渲染替代 video 显示，彻底解决交互问题
b212d94 修复：白名单→黑名单 + 点击兜底创建虚拟物体
0a197ca 优化：降低检测阈值至 0.05，提高小物体/手持物体识别率
c990c41 修复：摄像头交互改为点击层，禁用 PIP/右键/全屏
5402684 初始提交：AI 实时受力分析系统 v2.0
```

---

## 十三、后续开发建议

1. **换用 yolo11s 模型**：如果网络允许下载 18MB 模型，精度提升明显
2. **WebSocket 流式传输**：替代 HTTP POST 每帧传输，降低延迟
3. **SAM2 精确分割**：用户点击后获取物体精确轮廓
4. **深度估计**：添加 Depth Anything 实现 3D 空间理解
5. **物理参数标定**：根据镜头焦距标定像素/米比例
6. **物体质量库**：常见物体默认质量（水瓶≈0.5kg，手机≈0.2kg）
7. **双面摄像头选择**：支持切换前/后摄像头
